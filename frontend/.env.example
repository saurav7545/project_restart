# Leave unset for local Vite development (it uses the /api proxy).
# # For an Android release, use your deployed HTTPS backend URL, including /api.
VITE_API_BASE_URL=https://project-restart-gzin.onrender.com/
